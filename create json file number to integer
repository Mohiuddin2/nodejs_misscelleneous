data = [{jsong data will be here}]

data.table = []

// form making the number to string
  data.forEach(function (item) {
    let flat = item.flatNo;
    let aflat = `${flat}`;
    item.flatNo = aflat;
  });


fs.writeFile ("input.json", JSON.stringify(data), function(err) {
    if (err) throw err;
    console.log('complete');
    }
);
